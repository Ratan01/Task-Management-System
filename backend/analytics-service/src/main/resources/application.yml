server:
  port: 8086

spring:
  application:
    name: analytics-service

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}

app:
  internal:
    token: ${INTERNAL_TOKEN:}

management:
  endpoints:
    web:
      exposure:
        include: health,info